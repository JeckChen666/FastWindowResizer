# 第一版验证记录

日期：2026-09-13。

环境：Windows 11 专业版 x64，Build 26200；.NET SDK 10.0.401，运行时 10.0.12。使用 Release 构建。

## 已完成

- 自包含 win-x64 发布成功，编译输出无警告或错误。
- `build.ps1 -Test` 的 25 项检查全部通过。
- 几何计算覆盖负坐标显示器、大窗口、小工作区、DPI 阈值、固定尺寸窗口。
- 实际受控窗口覆盖屏幕外、最小化、最大化、异常缩小、固定尺寸、多个同进程窗口、隐藏窗口和活动模态对话框。
- 窗口关闭、身份不匹配均能安全返回错误。
- 真正阻塞消息处理的测试窗口约 264 ms 内返回操作受限或无响应提示，工具没有无限等待。
- 弹出菜单位于工作区内，已查看测试生成的菜单图像，中文标题、图标与长列表滚动入口正常显示。
- 菜单预热后连续重建和清理 60 次：进程句柄 534 → 534，GDI 对象 39 → 39，USER 对象 40 → 41，未见随刷新次数持续增长的迹象。
- 实际发布程序成功启动；再次启动的进程在 3 秒内以退出码 0 结束，原实例继续运行。

测试仅移动自行创建的窗口，没有对用户真实应用执行找回操作。菜单图像和测试输出位于测试构建目录，不包含在便携发布包中。

## 资源实测

实际发布进程启动等待 3 秒后，采样约 10 秒：

| 指标 | 结果 |
| --- | --- |
| 工作集 | 36.69 MiB |
| 私有内存（PrivateMemorySize64） | 8.64 MiB |
| 进程句柄 | 290 |
| CPU 时间增量 | 0 ms |
| 便携文件夹体积（文档复制前） | 108.73 MiB，219 个文件 |

工作集包含共享页，私有内存指标也不等于驻留的私有物理内存。以上是本机一次短时采样，不代表所有电脑或长期运行结果。没有通过清空工作集降低读数。

## 仍需实际使用验证

- 不同 DPI 的真实多显示器之间切换，以及远程断开重连、显示器实际断开。
- 用户常用应用与 Alt+Tab 列表逐项比对。
- 管理员应用的权限边界、特殊全屏应用及自行限制位置的应用。
- 资源管理器真正重启后的托盘恢复。实现使用 WinForms NotifyIcon 自带的 TaskbarCreated 处理，本次未重启用户的资源管理器。
- 多虚拟桌面的实际切换。
- 长期运行及频繁实际打开菜单后的内存变化。

上述项目未通过修改用户显示设置、重启桌面外壳或操作真实工作窗口来强行测试。

## 开机自启增量验证

- 新增当前用户范围的“开机自启”托盘开关，默认不自动开启。
- Release 编译成功：0 个警告，0 个错误。
- 10 项针对性检查全部通过：初始关闭、跨实例保存、空格及中文路径引号、REG_SZ 类型、旧位置检测与更新、删除自启值、保留其他值、重复关闭，以及过长路径拒绝。
- 检查使用随机命名的隔离注册表键，结束后删除，不在真实 Run 位置注册测试程序。
- 验证命令：`dotnet run --project tests/FastWindowResizer.Checks.csproj -c Release -- --startup-only`。
- 完整检查集现包含原来的 25 项窗口检查和新增的 10 项自启检查；本次按修改范围运行了新增检查，并成功编译应用。
- 未注销或重启用户电脑；真实登录后的启动行为尚待下次登录验证。任务管理器禁用启动项的状态不由本工具改写，相关说明已加入 README。
- 实现依据：[微软 Run 注册表项说明](https://learn.microsoft.com/en-us/windows/win32/setupapi/run-and-runonce-registry-keys)。

## 项目整理与 Git 入库验证

- 将暂存区中的文件导出至独立临时目录，不复制工作区中的 bin、obj、dist 或 output。
- 从导出的源码成功构建自包含 win-x64 发布包，确认所有编译资源与文档均已纳入版本管理。
- 在该目录通过 10 项自启配置检查，并成功从保留的原图重新导出 7 尺寸 ICO。
- 暂存内容通过 Git 空白检查和常见密钥格式扫描；编译产物、发布包及图像生成中间副本未入库。

## v1.0.0 发布验证

- 2026-09-13 在 Windows 11 上重新运行完整检查集：35 项全部通过。
- 无响应窗口检查约 256 ms 返回；60 次菜单刷新前后进程句柄 411 → 411、GDI 33 → 33、USER 32 → 33。
- 应用版本号为 1.0.0，面向 win-x64 生成自包含便携包；随包复制本次构建所使用 .NET 运行时的许可及第三方声明。
- 源码历史通过常见凭据格式检查，提交的 PNG 不包含附加文本元数据。
